# Studio Account Sync TODO 30

## Goal

把主脑、角色、风格库、插件、用户画像、自主进化记录，从“页面内散落状态”持续抽成“可同步、可分享、可治理”的账号资产层。

## Todo

- [x] 1. 建立统一 `StudioUserAssetApi`，禁止新增用户级资产直连 `localStorage`。
- [x] 2. 把角色补充词 addon 全量迁移到统一用户资产结构。
- [x] 3. 把自动角色草稿 draft 全量迁移到统一用户资产结构。
- [x] 4. 为主脑长期偏好建立独立资产结构，不再混在某个角色 addon 里。
- [x] 5. 把主脑长期偏好注入主脑 prompt、规划 prompt、路由 prompt。
- [x] 6. 为自定义风格库建立统一用户资产结构。
- [x] 7. 在画板节点保存自定义风格库时，同时写入用户风格库资产池。
- [x] 8. 给自定义风格库补稳定 `id`、`slug`、`updatedAt`、`createdBy` 字段。
- [x] 9. 建立“内置风格库 + 用户风格库 + 运行时临时风格库”的基础分层读取入口。
- [x] 10. 给角色资产建立“内置角色 + 用户角色 + 运行时临时角色”的分层读取模型。
- [x] 11. 给主脑资产建立“系统基线 + 用户长期偏好 + 当前任务临时脑”的分层拼装模型。
- [x] 12. 为角色、风格库、主脑偏好建立统一版本号和迁移机制。
- [x] 13. 把角色查看/编辑 UI 改为走统一用户资产 API，而不是直接读写本地键值。
- [x] 14. 把风格库查看/编辑 UI 改为走统一用户资产 API。
- [x] 15. 把自动组织出的临时角色支持“保存为正式角色”。
- [x] 16. 把临时生成的风格库支持“保存为正式风格库”。
- [x] 17. 支持主脑根据场景判断“复用已有角色 / 改写已有角色 / 新建临时角色”。
- [x] 18. 支持主脑根据场景判断“复用已有风格库 / 改写已有风格库 / 新建临时风格库”。
- [x] 19. 为用户画像建立账号级资产结构，存用户偏好、常见任务、审美习惯。
- [x] 20. 为主脑自主进化记录建立资产结构，记录哪些策略被验证有效。
- [x] 21. 为进化记录建立人工确认闸门，避免主脑误学习污染系统。
- [x] 22. 把模型偏好、浏览器调研偏好、工作台偏好逐步迁移到账号资产层。
- [x] 23. 把插件配置与 skill 偏好纳入账号资产层。
- [x] 24. 为角色、风格库、插件建立导出/导入格式，便于分享。
- [x] 25. 为角色、风格库、插件建立分享元信息结构，如作者、标签、摘要、兼容版本。
- [x] 26. 为用户资产 API 准备本地实现和远端实现双适配层。
- [x] 27. 为账号同步冲突建立合并策略，例如“本地较新优先 / 服务端优先 / 手动合并”。
- [x] 28. 为用户资产变更建立审计日志与回滚入口。
- [x] 29. 清理旧模块链路，禁止新功能继续把长期资产写回旧状态链。
- [x] 30. 给这整套资产层补自动化测试、迁移测试、构建校验与文档规范。
