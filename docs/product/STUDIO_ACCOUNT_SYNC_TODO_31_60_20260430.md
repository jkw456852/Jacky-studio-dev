# Studio Account Sync TODO 31-60

## Goal

在已完成“统一用户资产 API + 主脑/角色/风格库三层分层”的基础上，继续把资产体系推进到“可正式管理、可远端同步、可分享、可审计、可进化”的阶段。

## Todo

- [ ] 31. 把风格库查看 UI 完整切到 `StudioUserAssetApi`，不再混用节点临时状态和页面局部快照。
- [ ] 32. 给风格库编辑入口补“内置 / 用户 / 临时”来源标识，避免用户看不出当前改的是哪一层。
- [ ] 33. 给用户风格库补详情查看入口，能直接看到摘要、参考图解释、planning 指令、prompt 指令。
- [ ] 34. 给用户风格库补编辑保存能力，保存后统一回写资产层并刷新当前节点引用。
- [ ] 35. 给临时风格库补“保存为正式风格库”动作，保存时自动生成稳定 `id`、`slug`、`updatedAt`。
- [ ] 36. 给风格库补“另存为新版本”能力，避免直接覆盖已有常用版本。
- [ ] 37. 给风格库补删除能力，并在删除前检查是否仍被当前项目节点引用。
- [ ] 38. 给风格库补搜索、排序、最近使用排序，降低后期资产变多后的查找成本。
- [ ] 39. 给风格库补用途说明字段，例如“适合商品棚拍 / 海报重建 / 白底锁主体”。
- [ ] 40. 让主脑在选择风格库前先读取用途说明，再决定复用、改写还是新建临时风格库。
- [ ] 41. 给自动组织出的临时角色补“保存为正式角色”动作，并写入统一资产层。
- [ ] 42. 为正式角色资产建立 Markdown 化存储方案，与内置角色目录结构保持一致。
- [ ] 43. 给用户角色补用途说明字段，例如“适合修图统筹 / 电商详情页 / 强品牌保护场景”。
- [ ] 44. 给角色查看面板补“系统基线 / 主脑层 / 用户角色层 / 运行时临时层”的分层可视说明。
- [ ] 45. 让主脑在路由前先检索已有角色用途说明，优先复用合适角色而不是频繁新建临时脑。
- [ ] 46. 给“改写已有角色”建立显式数据结构，区分“正式改写”与“仅本次任务临时覆盖”。
- [ ] 47. 给角色资产补版本号与迁移字段，为后续账号同步和分享做准备。
- [ ] 48. 给角色资产补删除保护，防止删除后仍有路由规则或固定 pin 引用它。
- [ ] 49. 给角色资产补导出格式定义，至少包含 prompt、用途说明、作者、版本、兼容信息。
- [ ] 50. 给角色资产补导入校验，避免不完整或高风险角色定义直接进入主系统。
- [ ] 51. 为主脑长期偏好建立正式版本结构，支持后续迁移与冲突合并。
- [ ] 52. 为“当前任务临时脑”建立显式结构，不再只依赖零散 metadata 拼接。
- [ ] 53. 给主脑面板补“长期偏好”和“本次任务临时脑”边界说明，避免用户误把一次性指令写进长期层。
- [ ] 54. 给主脑补“建议沉淀为长期偏好”的候选机制，但默认不自动写入，必须先进入候选池。
- [ ] 55. 为主脑自主进化记录建立统一资产结构，记录提案来源、适用边界、验证结果、人工确认状态。
- [ ] 56. 建立主脑进化提案候选池 UI，先看提案摘要、证据、风险，再决定是否采纳。
- [ ] 57. 建立主脑进化的人工确认闸门，未确认前不得直接污染正式长期偏好。
- [ ] 58. 给主脑长期偏好补回滚入口，允许恢复到上一个稳定版本。
- [ ] 59. 让视觉规划链路能读取主脑进化中“已批准”的策略记录，但忽略未批准候选。
- [ ] 60. 给主脑层、角色层、风格库层补最小自动化测试，覆盖读取、写入、迁移、组合和回滚关键路径。
